"use server"

export default async function FilesPage() {
  return (
    <div className="bg-background flex h-full w-full items-center justify-center p-2">
      <p className="bg-muted text-muted-foreground border p-2 text-xs font-medium">Open a file</p>
    </div>
  )
}
