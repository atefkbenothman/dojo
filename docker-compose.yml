services:
  backend:
    container_name: backend
    build:
      context: .
      dockerfile: ./apps/backend/Dockerfile
    env_file:
      - apps/backend/.env
    image: ${DOCKER_IMAGE_TAG}
    ports:
      - "8888:8888"
    environment:
      - NODE_ENV=production
      - PORT=8888
    restart: always
